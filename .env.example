# Copia este archivo como ".env.local" para desarrollo local y configura
# la misma variable en Vercel (Project Settings > Environment Variables)
# para producción.

# URL base de la API (backend simpatizantes-backend en Render)
NEXT_PUBLIC_API_URL="https://simpatizantes-backend.onrender.com/api"
